Pre-processed images for clustering; Deployed K-means algorithm to perform image classification on MNIST dataset. 
Used common metrics to evaluate cluster performance & Visualized high-dimensional cluster centroids

